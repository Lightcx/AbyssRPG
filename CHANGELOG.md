# 变更记录 · Changelog

本文件记录《暗影深渊》的每个版本改了什么。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：

* **主版本**（x.0.0）：存档结构或核心玩法不兼容的大改
* **次版本**（0.x.0）：新系统、新内容
* **修订号**（0.0.x）：修 bug、数值调整、界面微调

> 版本号写在 `js/core.js` 的 `G.VERSION`，必须与下面最新一条标题一致（`tools/smoke.js` 会校验）。
> **每次改动都要升版本号，并在下面新增一条对应版本的记录**——已经写好的版本条目只做勘误，不再往里追加新内容；改动同时要同步 `README.md` 顶部的「当前版本」。
> 拿不准这次算次版本还是修订号（甚至主版本）时，**先问，不要自己拍**。
> 起始界面、暂停界面与错误覆盖层都会显示当前版本。
> 仓库地址：<https://github.com/Lightcx/AbyssRPG>；`Todo.txt` 里是待办清单，做完一条就删一条。

---

## [0.2.0] — 装备对比（秒伤 / 坚韧）、提示框价值显示、做装调整

从 0.1.0 之后的第二批改动：装备对比多出「秒伤 / 坚韧」两把尺子，提示框补齐评分与价值，做装工作台按参考图调整，外加一批显示与文案的修正。

### 新增

* **装备对比里加上「秒伤」与「坚韧」**：
  * **武器看秒伤**：提示框直接列出这把武器自己的每秒伤害，与已穿戴武器对比时左右两窗各带 `▲ / ▼` 差值徽章，换武器值不值一眼可见；空槽位不编造差值。
  * **护甲 / 副手 / 首饰看坚韧**：定义是「满血时能承受的减伤前伤害」，按 `生命 ÷ (1 − 减伤) ÷ (1 − 闪避) ÷ (1 − 受到伤害降低)` 逐系计算（物理用护甲减伤，火 / 冰 / 电 / 毒用各系抗性），**总坚韧 = 五类伤害的平均值**。对比时给出换装后的总坚韧与差值徽章。
  * **角色面板只显示总坚韧**，鼠标悬停「坚韧」才展开分项：五类伤害各自的坚韧与减伤，以及生命上限 / 护甲 / 闪避 / 受到伤害降低等计算输入。背包面板的属性摘要也加了坚韧一行。换装试算不会影响当前血量与法力。
* **提示框显示「装备评分 / 金币价值 / 残晶价值」**：名称行右侧是本件装备的评分（与评分对比用同一套权重），底部一行是出售金币与分解残晶（**只算基础值，不含铁匠铺升级的额外残晶**）。左右对比时两个窗口各自显示，方便比较两件装备谁更值钱。
* **传说石可以重复使用**：对已经注入过暗金特效的装备再用一次，会**刷新**成另一条暗金特效（不会掷回同一条），词缀与稀有度保持不变；日志会写明是「刷新暗金特效」。天然掉落的暗金装备不受影响（不会被打散重掷）。

### 改动

* **做装工作台的通货石换成参考图的环绕顺序**（正上方起顺时针）：净化石 → 增幅石 → 晋升石 → 炼化石 → 点金石 → 裂解石 → 混沌石 → 传说石 → 钻孔石。顺序集中在 `UI.BENCH_ORDER`，掉落权重等仍以 `D.ORBS` 为准。
* **通货石文案精炼**：九条描述重写为更短的说明（例：净化石「抹去魔法与稀有装备上的所有词缀，将其还原为普通装备。」、混沌石「重掷装备上的全部词缀。不改变装备稀有度或孔位。」），传说石的两句描述分行显示。
* **拾取提示带类别标签**：装备按稀有度显示（`拾取 [稀有] 炽炎之握`），宝石显示 `拾取 [宝石] 碎裂的红宝石`，做装通货显示 `拾取 [通货] 裂解石`，各有专属颜色。
* **精简无头冒烟测试**：断言条目 1137 → 948。按迭代重复的条目（技能分支档位、词缀前后缀归类、过滤器 20 条规则上限、装备栏部位、孔位上限等）合并为一条聚合断言（失败时仍会列出具体是哪些项）；删掉恒真断言（`ok(true, …)`、`… || true`）与 16g / 16l 之间重复覆盖的孔位渲染检查。

### 修复

* **武器秒伤漏算「附加的额外伤害」**：武器上「附加 X 点火焰 / 冰冷 / 闪电 / 毒素伤害」的词缀没有计入每秒伤害（角色面板与提示框都偏小），现在按 0.9 折算进秒伤并同样吃攻速加成（与角色面板的 `st.weaponDps` 算法对齐）。
* **戒指提示框的部位名**：戒指的物品槽位是 `ring` 而装备栏是 `ring1 / ring2`，查表查不到就退化成了「装备」——现在显示「戒指 · 精制」，与武器 / 护甲一致。
* 悬停身上正穿着的那件装备时，不再显示「和它自己比」的 `＝` 差值（其它部位照旧给出换装差值）。
* 做装通货石的拾取提示以前会显示成 `拾取 [undefined] 裂解石`，现在显示 `拾取 [通货] 裂解石`。

---

## [0.1.0] — 深渊层数与难度分离、装备过滤器、层缓存

按 `Todo.txt` 的 10 条改造全部完成（含第 5 条按参考图重排装备界面）。

### 新增

* **装备过滤器**（背包里点「过滤器」打开）：最多 20 条自定义规则，靠前的规则优先生效；每条规则可包含多条细则，细则类型包括物品等级、稀有度、装备部位、装备类型、包含词缀、名称、需求等级 / 力量 / 敏捷 / 智力、词缀条数、词缀品质、孔位数、是否双手、当前是否穿得上、评分。规则动作为「显示 / 高亮显示 / 隐藏」。支持**导出、导入**（JSON 文本，可复制备份或分享），配置对所有存档通用。
* **难度等级与深渊层数彻底分离**，难度选择放进**深渊之门**面板：
  * **层数**只决定怪物等级（难度只在其上再加 0-5 级）；
  * **难度**在其之上加成怪物生命 / 伤害 / 掉落率 / **掉落品质**；
  * 难度从 5 档扩到 **6 档**（普通 → 专家 → 噩梦 → 地狱 → 炼狱 → 湮灭）；
  * **解锁靠击败领主**：在当前难度击败「下一个 5 的倍数层」的深渊领主才解锁下一档（普通通关第 5 层 → 专家，专家通关第 10 层 → 噩梦……），不再随击杀自动跳档。
* **当前层深渊缓存**：回城或刷新页面后再进同一层，怪物（含血量与死亡状态）、地上的掉落、已破坏的物件、击杀进度、探索进度与玩家位置都会原样恢复；**只保留当前层**，进入别的层数或切换难度时旧缓存作废。
* **技能窗口显示升级收益**：打开技能强化窗口即可看到「投入下一点」的基础伤害 / 伤害倍率 / 增益数值变化，不必再回角色面板看提示框。

### 改动

* **装备栏改为参考图的排布**：3 列 4 行 —— **头盔居中在第一行**，下面三行依次是 `手套 | 胸甲 | 项链`、`主手 | 腰带 | 副手`、`戒指1 | 靴子 | 戒指2`。格子改为竖向方块（图标在上、名称在下、孔位菱形在底部居中），空槽位用虚线边框显示「部位 · 空」，鼠标悬浮有完整名称提示。**做装工坊左侧的装备栏沿用同一套排布**，两处保持一致；背包面板相应加宽到 880px。
* **掉落品质曲线重做**：由「层数 + 难度」共同推进，分 5 个阶段 —— 1-10 层以白装为主、蓝装少量、暗金是惊喜；11-25 层蓝装为主、词缀不满的黄装开始出现；26-40 层蓝黄并重、满词缀黄装出现；41-55 层黄装为主、白装基本绝迹；56 层以上以多词缀黄装与暗金为主。高阶段还会让黄装的词缀条数更容易掷满；物品等级与魔法装备掉落率在其上做微调。
* **深渊布局生成重写**：房间改为**网格化摆放**（每格最多一间、天然不重叠），走廊只连接相邻格子并走 L 形（宽度 2 = 一个门洞），保证全图连通，留出少量环路与死路分支。不再出现横穿全图的一堆乱路。
* **孔位上限按部位区分**：主手 / 副手 / 头盔 / 靴子 / 手套 / 腰带 **2 孔**，胸甲 **3 孔**，戒指 **1 孔**，项链 **2 孔**，**双手武器 4 孔**（= 主手 2 + 副手 2）。低等级装备还开不满孔，随物品等级逐步放开；孔位不再分两排显示。
* **界面上所有按键提示都会随改键自动更新**：右下角功能按钮的键位角标与悬浮说明、技能栏、任务提示行、地上的「按 X 进入下一层 / 交谈」提示、帮助面板、上下文菜单都改为实时读取当前绑定；改键、恢复默认、切换操作模式后立即刷新。
* **精简冗余描述**：去掉深渊之门面板下方的洗点指引、双手武器提示框里的「2 倍数值与孔位」说明、技能窗口里的重复提示。
* **平衡调整**：掉落品质提升后玩家后期输出是乘法叠乘成长，因此上调怪物成长以对齐 —— `monsterLifeExp` 1.023 → **1.044**、`monsterDmgExp` 1.026 → **1.028**、精英生命 ×3.0 → **×3.6**、BOSS 生命 ×6 → **×12**。
* **平衡探针同步新模型**：`tools/balance.js` 改为按「层数 × 难度」采样、按该进度的掉落阶段生成装备，并按怪物等级排序检查曲线平滑度。

### 修复

* 修正 DOM 引用检查的误判：`actionLabel('x')` 里的子串曾被当成 `el('x')`（`tools/smoke.js`）。

---

## [0.0.1] — 首个版本

原生 JavaScript + Canvas 的类《暗黑破坏神》刷宝 ARPG：无依赖、无构建，双击 `index.html` 即玩。

### 战斗与操作

* 三个职业（野蛮人 / 法师 / 猎魔人），各 5 个技能：1 个普通攻击 + 4 个主动技能。
* **两套操作模式**：鼠标模式（左键移动 / 右键普攻 / 1-4 技能）与 WASD 模式（WASD 移动 / 左键普攻 / 右键技能 1 / 1-3 技能 2-4），互不干扰。
* **鼠标移动死区**：光标离角色过近时停止移动，避免"原地抖动"。
* **按键重绑定**：设置面板可改键，两套模式各存一份；`Delete` 清除、`Esc` 放弃，同一按键在同一模式下只保留一个动作。
* 命中 / 暴击 / 减速 / 眩晕 / 易伤 / 持续伤害（流血、点燃、中毒）、击退、闪避、荆棘反弹。
* 14 种怪物 AI + 精英词缀；**5 个 BOSS**（血肉吞噬者 / 霜语者·维拉 / 焰喉 / 白骨之王 / 深渊编织者），各带 3-4 个专属技能，40% 生命以下进入狂怒。
* 每 5 层一位深渊领主，击杀后难度提升（普通 → 噩梦 → 地狱 → 炼狱 → 湮灭）。

### 技能强化分支与洗点

* 技能**不再在角色面板直接加点**，点击技能打开**强化窗口**：主线为技能等级，5 / 10 / 15 / 20 级各有两个互斥分支，25 级有三个更强的互斥分支（每个技能 11 个分支）。
* 分支效果覆盖技能伤害、攻击速度、法力消耗、冷却、作用范围、弹道数量、投射物速度与体积、穿透、持续时间、持续伤害、爆炸范围、增益强度、眩晕 / 减速 / 击退、暴击率与暴击伤害、对首领伤害、斩杀、生命偷取、击杀回法、元素转化、命中点燃 / 溅射等 20 余种。
* **解锁与生效都看实际技能等级**（含装备加成）：装备顶过档位即可分配；脱下装备掉回需求以下，分支保留但暂时失效（界面标红提示）。
* **25 级之后的成长递减**：仍可被装备顶高、数值继续提升，但没有新分支，且每级收益等比递减（26 级 95%、27 级 90.3%……），永远不会归零。
* **洗点**（城镇「深渊向导 · 塞拉」）：消耗金币 + 深渊残晶，可分别重置属性点、单个被动天赋、单个技能（并清空其分支）。费用随人物等级与**已投入点数**增长，装备提供的技能等级不计入。
* 塞拉首次交谈会**分 5 段**介绍世界观、加点规则、营地 NPC、深渊规矩与洗点规则。

### 装备与做装

* 稀有度：普通 → 魔法 → 稀有 → 暗金 / 传奇；11 类武器 × 8 个等级段、7 类护甲 + 盾牌 / 法球 / 箭袋 + 戒指项链。
* **41 条词缀（20 前缀 + 21 后缀）**，按物品等级分 8 档并做档间插值；魔法装最多 1 前 + 1 后，稀有装最多 3 前 + 3 后；前缀管进攻 / 属性 / 护甲，后缀管抗性 / 速度 / 续航 / 收益。
* **档位方向与流放之路一致：T1 最强、T8 最弱**，配色 `T1 红 · T2 粉 · T3-T4 青 · T5-T6 绿 · T7-T8 灰`；提示框显示每条词缀的档位、数值区间与品质条。
* **基础数值区间**：护甲显示 `下限 - 上限`；武器这类本身有浮动值的基底显示成两段区间，例如 `7-9 - 10-12`。
* **双手武器按「两手预算」给数值**：基底 DPS ≈ 同档单手的 2 倍、词缀数值 = 单手的 2 倍、孔位上限 = 单手 4 + 副手 4 = 8 孔（图标上 4 颗、下 4 颗菱形）。装上会顶掉副手，反向亦然，**评分对比会把被顶掉的那件算进损失**。
* **宝石镶嵌**：点到背包里的宝石即「拿起」，再点有孔位的装备完成镶嵌；珠宝匠可取下单颗或全部宝石；6 种宝石 × 5 个品质，可堆叠。
* **做装工作台**（秘法工匠维恩）：左侧物品栏 + 装备栏，点装备放上工作台；右侧装备居中、**9 种通货石环绕**。左键点通货「拿起」→ 点中央装备消耗改造。
* 通货石数量**自动统计背包 + 共享仓库 + 个人仓库**，消耗顺序为 背包 → 共享仓库 → 个人仓库；改造失败不消耗通货。
* 背包标识：装上后评分提升的装备右上角有绿色 ▲（会算上双手 / 副手互斥的损失），等级或属性不足的显示红框。
* 分解装备换深渊残晶；**怪物也会小概率直接掉落残晶**（普通约 5%、精英 22%、BOSS 75%），拾取不占背包并会被自动吸取。

### 城镇与成长

* 城镇「余烬营地」：铁匠铺（分解）、秘法工坊（做装）、深渊祭坛（城镇建设）、仓库（含跨存档共享货架）、训练场、集市、珠宝匠、深渊向导、深渊之门。
* 6 座可升级建筑（各 1→5 级，消耗金币 + 深渊残晶）：铁匠铺分解产出、秘法工坊通货石掉落与**残晶掉落率**、深渊祭坛属性、训练场经验、仓库容量、集市金币与魔法装备掉落。
* 每级 1 技能点 + 3 属性点，每 5 级 1 点被动天赋点。
* **3 个独立存档位** + 跨存档共享仓库（通货与宝石，每种上限 9999），自动存档。
* NPC 交互窗口在走远后自动关闭。

### 界面

* 暗黑风 UI：红蓝球、技能栏、小地图、伤害数字、地面光柱、动态光照。
* **装备对比双窗口**：悬停未穿戴装备时左右并列显示「已穿戴」与「新装备」，中间一条竖线分隔，评分结论固定在新装备窗口底部；戒指会同时对照两个戒指位。
* 深渊之门只管进入深渊、深渊向导只管洗点，职责分离。
* 起始界面、暂停界面与错误覆盖层都会显示当前版本号。

### 开发工具

* `tools/smoke.js`：无头冒烟测试（900+ 项断言），覆盖数据完整性、掉落生成、地牢生成、战斗与 BOSS、存档往返、全部 UI 面板、做装、技能分支、双手武器规则、残晶掉落、渲染层 NaN 防护、DOM 引用一致性与文件编码。
* `tools/balance.js`：数值平衡探针，输出三职业的击杀时间曲线。
* `tools/smoke-shim.js`：DOM / Canvas 无头垫片，其 canvas 会像浏览器一样拒绝 `NaN` 参数。
